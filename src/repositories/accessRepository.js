'use strict';

exports.authenticate = async (data) => {
    // const res = await Customer.findOne({
    //     email: data.email,
    //     password: data.password
    // });

    const result = {
        Name: "Rodolpho",
        Id: 1
    }

    return result;
}